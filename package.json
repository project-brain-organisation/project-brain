{
  "name": "project-brain-v2",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "api": "cd apps/api && npm run start:dev",
    "mcp": "cd apps/mcp && npm run dev",
    "web": "cd apps/web && npm run dev",
    "build:api": "cd apps/api && npm run build",
    "build:mcp": "cd apps/mcp && npm run build",
    "build:web": "cd apps/web && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^5.2.1"
  }
}
